{
  "name": "google-oauth2-client-promisify",
  "version": "1.0.9",
  "description": "Redesign the code that create OAuth2 client into promise chain and then wrap it as module, return promise with credentials which is thenable.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Carr <kaiboy6302@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/Carr1005/google-oauth2-client-promisify.git"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "google-auth-library": "^0.10.0"
  }
}
